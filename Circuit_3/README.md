This project is a reaction time tester using an LED and a pushbutton. The LED turns ON after a random delay generated by the program. When the LED lights up, the timer starts using the millis() function.
The user has to press the pushbutton as quickly as possible. The pushbutton is connected, so that it reads HIGH when not pressed and LOW when pressed.
